# SOA Architecture test 'Weather'

# Start

* Docker, docker-compose and make must be installed
* All commands must be run by user in docker group or root(not secure)

## Init application

### Build all images
```bash
make build
```
### Run tests
```bash
make run-tests
```
### Application start
```bash
make start
```
 
